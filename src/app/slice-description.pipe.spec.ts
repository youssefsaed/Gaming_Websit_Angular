import { SliceDescriptionPipe } from './slice-description.pipe';

describe('SliceDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
