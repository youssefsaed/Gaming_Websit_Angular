import { IconPlatformPipe } from './icon-platform.pipe';

describe('IconPlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new IconPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});
